#include "header.h"

int main() {
    string greeting="what is hell";
    // greeting.insert(3," ");
    // greeting.erase(greeting.length()-1);
    // greeting.pop_back();
    // greeting.replace(greeting.find("hell"), 4, "****");
    // if(greeting.find_first_of("!") == -1) cout << "NOT FOUND" << endl;
    if(greeting == "what is hell") cout << "Equals" << endl;
    return 0;
}

